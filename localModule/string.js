const firstname = "Monkey"
const middlename = "D."
const lastname = "Luffy"

function names(){
    const results = `${firstname} ${middlename} ${lastname}`
    console.log(results)
}

const nama = {
    names
}

module.exports = nama